{
  "name": "propellr",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json && vite build",
    "typecheck": "tsc -p tsconfig.portable.json && tsc -p tsconfig.browser.json && tsc -p tsconfig.host.json && tsc -p tsconfig.test.json && tsc -p tsconfig.browser-test.json",
    "lint": "oxlint --deny-warnings .",
    "format": "oxfmt .",
    "format:check": "oxfmt --check .",
    "test:contracts": "vitest run --project contracts",
    "browsers:install": "PLAYWRIGHT_BROWSERS_PATH=\"${PLAYWRIGHT_BROWSERS_PATH:-$PWD/.tools/browsers}\" playwright install chromium firefox webkit",
    "test:host": "pnpm build && PLAYWRIGHT_BROWSERS_PATH=\"${PLAYWRIGHT_BROWSERS_PATH:-$PWD/.tools/browsers}\" vitest run --project host",
    "test:playbooks": "pnpm build && PLAYWRIGHT_BROWSERS_PATH=\"${PLAYWRIGHT_BROWSERS_PATH:-$PWD/.tools/browsers}\" vitest run --project playbooks",
    "reference:prepare": "node tools/reference.ts",
    "test:parity": "pnpm build && PLAYWRIGHT_BROWSERS_PATH=\"${PLAYWRIGHT_BROWSERS_PATH:-$PWD/.tools/browsers}\" vitest run --project parity --project browser",
    "test:reporting": "vitest run --project reporting",
    "bench:slice": "pnpm build && PLAYWRIGHT_BROWSERS_PATH=\"${PLAYWRIGHT_BROWSERS_PATH:-$PWD/.tools/browsers}\" vitest run --project bench",
    "validate": "pnpm build && pnpm typecheck && pnpm lint && pnpm format:check && pnpm test:contracts && pnpm test:host && pnpm test:playbooks && pnpm test:parity && pnpm test:reporting"
  },
  "dependencies": {
    "playwright": "1.63.0",
    "zod": "4.6.2"
  },
  "devDependencies": {
    "@types/node": "26.5.1",
    "@vitest/browser-playwright": "5.0.0",
    "oxfmt": "0.67.0",
    "oxlint": "1.82.0",
    "typescript": "7.0.2",
    "vite": "8.3.0",
    "vitest": "5.0.0"
  },
  "engines": {
    "node": "26.8.2",
    "pnpm": "12.4.1"
  },
  "packageManager": "pnpm@12.4.1"
}
